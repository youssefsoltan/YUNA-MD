/*let handler = m => m
handler.all = async function(m) {
  if (/^hi$/i.test(m.text)) {
    conn.reply(m.chat, 'Hello!')
  } else if (/^hello$/i.test(m.text)) {
    conn.reply(m.chat, 'Hi there!')
  } else if (/^hey$/i.test(m.text)) {
    conn.reply(m.chat, 'Hello! How are you doing?')
  }
  return !0
}
export default handler
*/